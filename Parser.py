__author__ = 'pascal'

class Parser:

    @staticmethod
    def extract_tokens_from_set(set):

        for str in set:
            tokens = list(map(lambda word: word.lower(), str.split()));

        pass

    @staticmethod
    def tokenize_set(set):
        f
        pass